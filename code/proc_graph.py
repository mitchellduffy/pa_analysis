

class ProcGraph:

    def __init__(self, ml, sig, norm, ener):
        #self.ml = ml
        #signal = self.collect(sig, ener)
        #if norm !< 0:
        #    normalize = self.collect(norm, ener)
        pass




